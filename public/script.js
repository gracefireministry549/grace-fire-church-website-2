function toggleMenu(){document.getElementById('nav').classList.toggle('show')}
document.querySelectorAll('#nav a').forEach(a=>a.addEventListener('click',()=>document.getElementById('nav').classList.remove('show')));
const target=new Date('2026-08-30T00:00:00+01:00').getTime();
function countdown(){const d=target-Date.now();if(d<=0){['days','hours','minutes','seconds'].forEach(id=>document.getElementById(id).textContent='0');return}document.getElementById('days').textContent=Math.floor(d/86400000);document.getElementById('hours').textContent=Math.floor(d%86400000/3600000);document.getElementById('minutes').textContent=Math.floor(d%3600000/60000);document.getElementById('seconds').textContent=Math.floor(d%60000/1000)}countdown();setInterval(countdown,1000);
const form=document.getElementById('prayerForm');form.addEventListener('submit',async e=>{e.preventDefault();const msg=document.getElementById('prayerMessage');const data=Object.fromEntries(new FormData(form).entries());msg.textContent='Sending...';try{const r=await fetch('/api/prayer-request',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const result=await r.json();msg.textContent=result.message;if(result.success)form.reset()}catch(err){msg.textContent='Could not send the request. Please try again.'}});
